# Task List — Remove Notes, Capture, LIFE AI, Intelligence, Insights, Search, Weekly Rhythm & AI settings

## Phase 1 — Plan (DONE)
- [x] Audit dependency map of deleted systems (authoritative prompt = this scope; no prior plan).
- [x] Write `tasks/plan.md` and this todo list.

## Phase 2 — Refactor retained code
- [x] `core/di/ServiceLocator.kt` — drop note/capture/intelligence/ai/life deps; rewire backup + timeline.
- [x] `domain/usecase/BuildTimelineUseCase.kt` — drop NOTE/CAPTURE sources.
- [x] `domain/model/TimelineItem.kt` — drop NOTE/CAPTURE enum cases.
- [x] `ui/timeline/TimelineScreen.kt` — remove capture repository/thumbnail/click.
- [x] `data/repository/BackupRepository.kt` — drop notes/captures from `LifeOSBackup`, constructor, build/restore; keep formatVersion=1.
- [x] `ui/home/HomeScreen.kt` — drop entry points (capture/AI/notes/insights/search), FAB, Notes tile, reword copy, trim `iconFor`.
- [x] `ui/diary/DiaryViewModel.kt` + `DiaryScreen.kt` — drop intelligence/timeline; remove analytics/connections/ask UI + keyword chips.
- [x] `ui/diary/DiaryDetailScreen.kt` (+ inline VM) — drop graph + keywords + MoodAnalyzer fallback in `EntryMoodPill`.
- [x] `ui/diary/DiaryMoods.kt` — remove `fromAnalysis` + `Mood` import.
- [x] `ui/habits/HabitsScreen.kt` — remove "Weekly Rhythm" card + now-unused `completionPercent`/`todayDone`/`BoxCentered`.
- [x] `core/util/SettingsStore.kt` + `ui/settings/SettingsScreen.kt` — remove AI features toggle/key/flow/setter + "Privacy & Local Intelligence" section; reword on-device copy.
- [x] `ui/onboarding/OnboardingScreen.kt` — drop AI page; update copy/icons.
- [x] `ui/navigation/` — prune `Screen.kt`, `LifeOSNavHost.kt`, `LifeOSBottomBar.kt`; delete `LifeDestinationRoutes.kt`.
- [x] Repositories/DAOs: `TaskRepository`+`TaskDao`, `ExpenseRepository`+`ExpenseDao`, `DiaryRepository`+`DiaryDao` — strip AI/search/range-only methods.
- [x] `core/util/PermissionManager.kt` — remove `LifeOSPermissions`.
- [x] `core/util/MediaStorage.kt` — remove captures methods.
- [x] `data/db/Converters.kt` — remove `CaptureType` converters.
- [x] `AndroidManifest.xml` — drop CAMERA/RECORD_AUDIO/uses-feature; fix comments.
- [x] `res/xml/file_paths.xml` — drop captures path.
- [x] `res/values/strings.xml` — reword tagline.
- [x] `app/build.gradle.kts` — drop CameraX block; fix coil comment.

## Phase 3 — Delete deleted-feature code
- [x] Delete dirs `core/ai/`, `core/life/`, `core/intelligence/`, `ui/ai/`, `ui/capture/`, `ui/notes/`, `ui/insights/`, `ui/search/`.
- [x] Delete `domain/model/NoteBlock.kt`, `data/db/entities/{NoteEntity,CaptureEntity}.kt`, `data/db/dao/{NoteDao,CaptureDao}.kt`, `data/repository/{NoteRepository,CaptureRepository}.kt`, `ui/navigation/LifeDestinationRoutes.kt`, `ui/diary/{DiaryAnalyticsSection,DiaryConnectionsView}.kt`.
- [x] Delete tests: NoteTextAnalyzer, MoodAnalyzer, DiaryConnections, SearchCategory, NarrativeScore, LifeDestinationRoutes.

## Phase 4 — Room migration v2 → v3
- [x] `data/db/AppDatabase.kt` — version 3, drop `noteDao()`/`captureDao()`, remove entities, add `MIGRATION_2_3` (drop notes + captures tables).
- [ ] `app/schemas/.../3.json` generated by build.
- [x] Migration tests — JVM schema test (3.json lacks notes/captures) + instrumentation `MigrationTestHelper` (compile via `assembleDebugAndroidTest`; note device requirement).

## Phase 5 — Verification
- [ ] `./gradlew :app:tasks` warmup; `./gradlew :app:assembleDebug`; `:app:testDebugUnitTest`; `:app:lintDebug` — all pass, no suppression.
- [x] Tests updated: `BackupSerializationTest` (drop notes/captures, keep legacy-decode test), `BottomNavItemsTest` (Home/Tasks/Habits), `DiaryMoodsTest` (drop fromAnalysis). `DailyUpdatePercentTest` unchanged.
- [ ] Final self-review of full diff for leaks (grep for removed symbols).
- [ ] Final self-review of full diff for leaks (grep for removed symbols).

## Phase 6 — Docs, commit, PR
- [ ] Append to UPDATE.md (scope, migration, data preservation, testing, known issues).
- [ ] Stage only intended files; commit on `feat/remove-notes-capture-life-intelligence`.
- [ ] Prepare PR (title/description per spec) against `origin/main`.
- [ ] STOP — ask user for GitHub PAT before any push. Never write PAT to disk.